/**
 * The name the game ships under. Shown small on the title screen and written
 * into every save's `meta`, so a file says which build made it. The frontier
 * ships as Beta 1 (STATUS §Decisions, 2026-09-25); bump it here and nowhere else.
 */
export const GAME_VERSION = 'Beta 1'
